{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row text-center bg-head">
            <div class="col-lg-12">
                <img class="img-fluid main-logo" src="{{ asset('images/logo-conparss.png') }}">
            </div>
        </div>
        <div class="text-center title">
            <h1>Avec CONPARSS, tu te désabonnes plus rapidement que tu t'abonnes</h1>
        </div>
        <div class="text-center">
            {{ form_start(form) }}
            {{ form_row(form.imageFile) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
